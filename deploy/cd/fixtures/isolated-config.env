# Synthetic-only configuration for the D1 harness. It is deliberately not a
# C00 configuration snapshot and cannot qualify an activation.
DATABASE_URL=postgres://multica:synthetic-only@postgres:5432/multica?sslmode=disable
JWT_SECRET=qualification-synthetic-only
MULTICA_PUBLIC_URL=http://127.0.0.1:18882
